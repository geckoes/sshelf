[project]
name = "sshelf"
version = "0.1.0"
description = "A lightweight TUI to manage your SSH config easily"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "textual>=8.2.4",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
